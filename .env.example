# Server
PORT=3000
NODE_ENV=development

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
DB_DATABASE=orm_dev

# Set to true ONLY in local dev to auto-sync schema (never in production).
# Prefer migrations instead.
DB_SYNCHRONIZE=false
DB_LOGGING=true
